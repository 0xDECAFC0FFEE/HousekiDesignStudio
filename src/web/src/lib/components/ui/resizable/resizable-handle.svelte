<script>
	import * as ResizablePrimitive from "paneforge";
	import { cn } from "$lib/utils.js";

	// The shadcn-svelte handle, with one change: no grip. The CLI's `withHandle` short bar (and the
	// three dots this file drew in its place until T-0226 -- the user, 2026-09-22: "i especially
	// dislike the bar with 3 dots") is gone; the seam itself is the handle, and a caller shows it
	// on hover and drag with its own classes (Workspace.svelte). Everything else is the CLI's file.
	let {
		ref = $bindable(null),
		class: className,
		...restProps
	} = $props();
</script>

<ResizablePrimitive.PaneResizer
	bind:ref
	data-slot="resizable-handle"
	class={cn(
		"relative flex w-px items-center justify-center bg-border after:absolute after:inset-y-0 after:left-1/2 after:w-1 after:-translate-x-1/2 focus-visible:ring-1 focus-visible:ring-ring focus-visible:ring-offset-1 focus-visible:outline-hidden data-[direction=vertical]:h-px data-[direction=vertical]:w-full data-[direction=vertical]:after:left-0 data-[direction=vertical]:after:h-1 data-[direction=vertical]:after:w-full data-[direction=vertical]:after:translate-x-0 data-[direction=vertical]:after:-translate-y-1/2",
		className
	)}
	{...restProps}
/>
